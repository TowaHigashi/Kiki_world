## これからの手順

# STEP1：まずは「街」を表示
※一旦超シンプルでいい
BoxGeometryで建物4つ
Planeで地面
ライト追加
カメラ固定

# STEP2：クリック判定（Raycaster）
Raycaster（超重要）
マウスクリック → 3Dオブジェクト取得
※クリックした建物が分かるようにする。console.logでOK

# STEP3：カメラ移動
OrbitControls（最初は自由操作）☑

・建物をクリックしたら、カメラがスーッと寄る
※現状のカメラ制御：controls.update()
① クリック
　　↓
② どの建物か取得（Raycaster）← もうできてる
　　↓
③ その建物の座標を取得
　　↓
④ カメラの「行き先」を決める
　　↓
⑤ 毎フレーム少しずつ移動（アニメーション）
　　↓
⑥ カメラを建物に向ける

STEP1：ターゲット変数を作る
「どこに行くか」を保存する（変数：targetPosition、targetLookAt、isMoving）

STEP2：クリック時にターゲットを更新
建物クリック → 目的地決定

STEP3：animateでカメラを移動
lerpでスムーズ移動☑

※ここまでは
クリック → selected.position ベースで自動計算
だったものを、
建物ごとにデータを持たせたい →→ u3-3

カメラ位置リセット機能 → u4-4

    → こっから後日やります
    // building.userData = config　追加した後に、建物ごとにデータを持たせる必要がある

```vue
createBuilding(-2, -2, 1.4, 1.4, 2.2, {
  cameraPosition: new THREE.Vector3(1, 2, 1),
  lookAt: new THREE.Vector3(-2, 1, -2),
  name: 'Building A'
})

createBuilding(1.8, -1.8, 2.5, 1.5, 3, {
  cameraPosition: new THREE.Vector3(4, 3, 1),
  lookAt: new THREE.Vector3(1.8, 1.5, -1.8),
  name: 'Building B'
})
```
データ構造 → この形にすることで、あとでvueと連携してサクッとUI表示できる

userData: {
  cameraPosition,
  lookAt,
  title: 'ポートフォリオ',
  description: 'ここに説明',
  link: '/works/1'
}

# STEP4：UI（説明文）
Vue側の状態管理でUI表示
「どの建物が選ばれたか」をstateで管理


## 最小構成

<template>
  <div ref="canvas"></div>

  <div v-if="selected">
    {{ selected }}
  </div>
</template>

## 設計イメージ（Vue + Three）

Vue（UI）
├── テキスト表示
├── 状態管理（selectedBuilding）

Three.js（描画）
├── シーン
├── 建物
└── カメラ制御

## markdown

// App.vue
```vue

```

## 後回し

・影を有効化（超重要）
renderer.shadowMap.enabled = true
・影を落とす設定
directionalLight.castShadow = true
・建物に影を出す
building.castShadow = true
building.receiveShadow = true
・地面に影を落とす
ground.receiveShadow = true

・カメラ位置リセット機能：u4-4
→ 別ファイルで管理したい



## キーワード

Raycaster：クリック判定
Vector3：座標
lerp or GSAP：アニメーション
lookAt：カメラ向き
OrbitControls：操作



 
PS C:\Users\yukim\OneDrive\デスクトップ\kikiwebsite\ThreeProject1> npm install 
npm error code ENOENT
npm error syscall open
npm error path C:\Users\yukim\OneDrive\デスクトップ\kikiwebsite\ThreeProject1\package.json
npm error errno -4058
npm error enoent Could not read package.json: Error: ENOENT: no such file or directory, open 'C:\Users\yukim\OneDrive\デスクトップ\kikiwebsite\ThreeProject1\package.json'
npm error enoent This is related to npm not being able to find a file.
npm error enoent
npm error A complete log of this run can be found in: C:\Users\yukim\AppData\Local\npm-cache\_logs\2026-04-02T15_08_53_616Z-debug-0.log
PS C:\Users\yukim\OneDrive\デスクトップ\kikiwebsite\ThreeProject1> 

App.vue
├── ThreeScene.vue
├── StartPage.vue
└── BackButton.vue
とすべきか、
App.vue
├── ThreeScene.vue
│   └── BackButton.vue
└── StartPage.vue
とすべきか、どっちが丸い？
「一定以上操作をしたら」という設計を組み込む以上ThreeScene.vueに依存する気がしていて


## 大元のコード // bkを用意したので不要

<template>
  <div ref="container"></div>
</template>

<script setup>
import { onMounted, ref } from 'vue'
import * as THREE from 'three'

const container = ref(null)

onMounted(() => {
  const scene = new THREE.Scene()

  const camera = new THREE.PerspectiveCamera(
    75,
    window.innerWidth / window.innerHeight,
    0.1,
    1000
  )

  const renderer = new THREE.WebGLRenderer()
  renderer.setSize(window.innerWidth, window.innerHeight)

  container.value.appendChild(renderer.domElement)

  const geometry = new THREE.BoxGeometry()
  const material = new THREE.MeshBasicMaterial({ color: 0x00ff00 })
  const cube = new THREE.Mesh(geometry, material)

  scene.add(cube)
  camera.position.z = 5

  const animate = () => {
    requestAnimationFrame(animate)
    cube.rotation.x += 0.01
    cube.rotation.y += 0.01
    renderer.render(scene, camera)
  }

  animate()
})
</script>
